import React from 'react';
import HeaderBar from "../components/HeaderBar";
import SearchForm from "../SearchForm";

function Root() {
  return (
    <div>
      <HeaderBar/>
      <SearchForm/>
    </div>
  );
}

export default Root;